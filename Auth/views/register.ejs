<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= editing ? "Edit" : "Register"%></title>
  <link href="/styles/output.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-blue-50 via-gray-50 to-slate-100 min-h-screen">

<div class="flex items-center justify-center min-h-screen p-4">
  <!-- Container -->
  <div class="bg-white/90 backdrop-blur-lg p-10 md:p-14 rounded-3xl shadow-2xl w-full max-w-xl border border-gray-200 transition-all duration-300">
    <!-- Header -->
    <div class="text-center mb-10">
      <div class="w-24 h-24 bg-gradient-to-r from-red-500 to-pink-500 rounded-full flex items-center justify-center mx-auto mb-6 shadow-lg">
        <svg class="w-12 h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
        </svg>
      </div>
      <h1 class="text-4xl font-extrabold mb-2 text-transparent bg-clip-text bg-gradient-to-r from-gray-800 to-gray-600"><%= editing ? "Edit Home" : "Register Your Home"%></h1>
      <p class="text-gray-500 text-lg">Share your beautiful space with the world</p>
    </div>
         
    <!-- Form -->
    <form action="<%= editing ? '/host/edit-' + home.name + '/' + home.id : '/host/register' %>" method="POST" class="space-y-7">

      <input type="hidden" name="id" value="<%= editing ? home.id : '' %>">
      <!-- House Name -->
      <div>
        <label for="name" class="block text-gray-700 font-semibold mb-2 text-sm uppercase tracking-wider">House Name</label>
        <input value="<%= editing ? home.name : '' %>" type="text" name="name" id="name" placeholder="Enter house name"
               class="w-full px-5 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-red-200 focus:border-red-400 transition-all duration-200 bg-gray-50 shadow-sm">
      </div>

      
             
      <!-- House Number -->
      <div>
        <label for="house_no" class="block text-gray-700 font-semibold mb-2 text-sm uppercase tracking-wider">House No</label>
        <input value="<%= editing ? home.house_no : '' %>" type="text" name="house_no" id="house_no" placeholder="Enter house number"
               class="w-full px-5 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-red-200 focus:border-red-400 transition-all duration-200 bg-gray-50 shadow-sm">
      </div>

      <!-- Price -->
      <div>
        <label for="price" class="block text-gray-700 font-semibold mb-2 text-sm uppercase tracking-wider">Price</label>
        <div class="relative">
          <span class="absolute left-5 top-1/2 transform -translate-y-1/2 text-gray-400 text-lg">₹</span>
          <input value="<%= editing ? home.price : '' %>" type="number" name="price" id="price" placeholder="Enter price"
                 class="w-full pl-10 pr-5 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-red-200 focus:border-red-400 transition-all duration-200 bg-gray-50 shadow-sm">
        </div>
      </div>
            
      <!-- Rooms -->
      <div>
        <label for="rooms" class="block text-gray-700 font-semibold mb-2 text-sm uppercase tracking-wider">Rooms</label>
        <input value="<%= editing ? home.rooms : '' %>" type="text" name="rooms" id="rooms" placeholder="Enter number of rooms"
               class="w-full px-5 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-red-200 focus:border-red-400 transition-all duration-200 bg-gray-50 shadow-sm">
       </div>

      <!-- Address -->
      <div>
        <label for="address" class="block text-gray-700 font-semibold mb-2 text-sm uppercase tracking-wider">Address</label>
        <input value="<%= editing ? home.address : '' %>" type="text" name="address" id="address" placeholder="Enter Address"
               class="w-full px-5 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-200 focus:border-blue-400 transition-all duration-200 bg-gray-50 shadow-sm">
       </div>

      <!-- Photo URL -->
      <div>
        <label for="photo" class="block text-gray-700 font-semibold mb-2 text-sm uppercase tracking-wider">Photo URL</label>
        <input value="<%= editing ? home.photo : '' %>" type="text" name="photo" id="photo" placeholder="Enter photo URL"
               class="w-full px-5 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-red-200 focus:border-red-400 transition-all duration-200 bg-gray-50 shadow-sm">
      </div>

      <!-- Submit Button -->
      <button type="submit"
              class="w-full bg-gradient-to-r from-red-500 to-pink-500 text-white font-bold py-3 rounded-xl hover:from-red-600 hover:to-pink-600 transition-all duration-200 shadow-lg transform hover:scale-105">
        <span class="flex items-center justify-center gap-2">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
          </svg>
          <%= editing ? "Edit " : "Register "%>
        </span>
      </button>
    </form>
    
    <!-- Footer -->
    <div class="mt-10 text-center">
      <p class="text-gray-500">
        Already registered? 
        <a href="/" class="text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-pink-500 font-semibold hover:underline transition-all">Go back home</a>
      </p>
    </div>
  </div>
</div>
</body>
</html>